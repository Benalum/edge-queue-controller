#!/usr/bin/env bash
set -euo pipefail

DOC="docs/stage-16-e3v-c-read-only-schema-capability-check-result.md"

echo "=== Stage 16 E3V-C-R2 smoke: documented read-only schema capability result ==="

test -s "$DOC"

grep -F "E3V_C_SCHEMA_CAPABILITY_RESULT=OPTION_B_SCHEMA_CAPABLE_FOR_NO_SCHEMA_ONE_SHOT_DESIGN" "$DOC"
grep -F "E3V_C_READ_ONLY_SCHEMA_CAPABILITY_OK" "$DOC"
grep -F "E3V_C_READ_ONLY_SCHEMA_CAPABILITY_CHECK_OK" "$DOC"
grep -F "HEAD/origin/main/remote: f912e81" "$DOC"
grep -F "NO_DB_WRITE" "$DOC"
grep -F "NO_SCHEMA_MIGRATION" "$DOC"
grep -F "DB_OPEN_MODE=sqlite_uri_mode_ro_immutable" "$DOC"
grep -F "DB_INTEGRITY=ok" "$DOC"
grep -F "SQLITE_VERSION=3.46.1" "$DOC"
grep -F "JOBS_COLUMN name=status" "$DOC"
grep -F "JOBS_COLUMN name=attempts" "$DOC"
grep -F "JOBS_COLUMN name=updated_at" "$DOC"
grep -F "OPTION_B_REQUIRED_COLUMNS_PRESENT=True" "$DOC"
grep -F "OPTION_B_REQUIRED_COLUMNS_MISSING=none" "$DOC"
grep -F "JOB_RESULTS_HAS_JOB_ID_COLUMN=true" "$DOC"
grep -F "JOB_RESULTS_UNIQUE_JOB_ID_INDEX_PRESENT=false" "$DOC"
grep -F "job_results.job_id is the primary key" "$DOC"
grep -F "OPTION_C_LEASE_COLUMNS_PRESENT=none" "$DOC"
grep -F "OPTION_C_MIGRATION_REQUIRED_FOR_DEDICATED_LEASE=true" "$DOC"
grep -F "JOBS_TOTAL=27" "$DOC"
grep -F "JOB_RESULTS_TOTAL=10" "$DOC"
grep -F "DUPLICATE_JOB_RESULTS none" "$DOC"
grep -F "JOB_CLASSIFY id=23 present=true status=queued" "$DOC"
grep -F "JOB_CLASSIFY id=24 present=true status=queued" "$DOC"
grep -F "JOB_CLASSIFY id=27 present=true status=completed attempts=1" "$DOC"
grep -F "JOB_CLASSIFY id=28 present=true status=completed attempts=1" "$DOC"
grep -F "Do not rerun job 27" "$DOC"
grep -F "Do not rerun job 28" "$DOC"
grep -F "OPTION_B_ATOMIC_SQL_SHAPE=UPDATE jobs SET status='running'" "$DOC"
grep -F "OPTION_B_SCHEMA_CAPABLE=true" "$DOC"
grep -F "OPTION_B_RUNTIME_APPLY_PERFORMED=false" "$DOC"
grep -F "PERSISTENT_SCHEDULER_ACTIVATION_ALLOWED=false" "$DOC"
grep -F "PERSISTENT_WORKER_ACTIVATION_ALLOWED=false" "$DOC"
grep -F "E3V-D no-apply Option B atomic-status-claim implementation plan" "$DOC"
grep -F "APPROVE_STAGE_16_E3V_RUN_ONE_EXISTING_STATUS_ATOMIC_CLAIM_DISPATCH_ONLY" "$DOC"
grep -F "The current schema is capable" "$DOC"

echo "E3V_C_R2_DOC_SMOKE_OK"
