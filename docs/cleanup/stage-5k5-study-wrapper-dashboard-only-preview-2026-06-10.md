# Stage 5K-5 Study Wrapper Dashboard-Only Preview — 2026-06-10

Updated /study-wrapper-preview to load a Study dashboard-only partial instead of the larger Study content partial.

The preview now shows the shared wrapper header plus Study dashboard panels only.

Study JavaScript is still intentionally not running in the preview, so counts remain static/default.

Live /study remains unchanged and still serves the standalone working Study dashboard.
