# Stage 5P-5B Study Session Command Lifecycle Integration Smoke

Adds a live integration smoke for the Study session command endpoint.

The smoke verifies the command endpoint can execute:

- `Study Session Start`
- `Study Session Pause`
- `Study Session Resume`
- `Study Session Stop`

This stage does not add runtime behavior. It only adds test coverage.
