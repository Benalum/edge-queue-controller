# Stage 5P-8A Study Session Status Card

Adds a frontend-only Study session status card.

The card reads:

- GET /api/study/session/status

The card displays:

- status
- deck id
- current card id
- queue position
- last action
- updated time

This stage does not add command buttons, does not execute Study commands, and does not change backend behavior.
