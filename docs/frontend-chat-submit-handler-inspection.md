# Frontend Chat Submit Handler Inspection — Stage 5F-42

Generated by read-only inspection.

No frontend runtime behavior changed.

## Git checkpoint

## main...origin/main
?? docs/frontend-chat-submit-handler-insertion-map.md
?? docs/frontend-chat-submit-handler-inspection.md
?? ops/smoke/check-frontend-chat-submit-handler-insertion-map.sh
6b3eb21 Add frontend queued chat submit decision mock test
3567726 Add disabled frontend queued chat submit decision branch
8b4e949 Document frontend queued chat first wiring plan
f49cdb2 Add frontend queued chat assistant placeholder mock test
6dc32fb Add disabled frontend queued chat assistant placeholder branch
e347f2d Add frontend queued chat status poll helper mock test
8f4320b Fix frontend queued chat status poll smoke regex
bec8b90 Add disabled frontend queued chat status poll branch

## app.js likely submit/send/chat markers
9:    text.includes("error code 502") ||
10:    text.includes("error code 503") ||
11:    text.includes("error code 504")
18:    return "The companion is still working or the gateway timed out before the browser received the final response. I will not show the raw Cloudflare error page. Refresh in a moment or try again.";
56:// can decide whether /study, /chat, /companion, /calendar, and /profile should proxy
71:    lower.includes("error code 502") ||
72:    lower.includes("error code 503") ||
73:    lower.includes("error code 504")
75:    return "The companion is still working or the gateway timed out before the browser received the final response. I will not show the raw Cloudflare error page. Refresh in a moment or try again.";
129:const PRIVATE_APP_ROUTE_SET = new Set(["/study", "/chat", "/companion", "/profile"]);
153:      ["Chat", "Use AI Chat or switch into Companion mode for study help, explanations, and supportive conversation.", "/chat"],
155:      ["System", "View platform capacity, API health, and resource state.", "/system"],
158:      ["Personalized practice", "The platform is designed around active recall, guided feedback, and focusing on weak areas."],
179:  "/chat": {
180:    eyebrow: "Unified chat",
183:      "Chat is the single place for normal AI Chat and Companion mode. Use AI Chat for general local Ollama conversations, or switch to Companion mode for study-aware support.",
185:      ["AI Chat", "General local-first AI chat with an experimental queued mode."],
186:      ["Companion mode", "Supportive companion conversation with study tools and safety boundaries."],
188:      ["Safe migration", "Queued chat stays opt-in and Companion remains synchronous for now."],
196:      "Companion is the AI helper for conversation, studying, explanations, practice, and future personalized support.",
210:      "Instead of storing a permanent built-in calendar, the platform will connect with Apple Calendar and Google Calendar when users choose to authorize it.",
224:      "Profile will manage account settings, preferences, permissions, and personalization for the platform.",
228:      ["Personalization", "Store preferences that help the platform adapt to each user."],
253:      "Send a message to support if something is not working, you need account help, or you have questions about credits and services.",
258:    eyebrow: "Platform status",
261:      "System summarizes platform infrastructure and API health without exposing private server details.",
308: * Note: The wrapper makes fetch calls to /api/* paths, which are translated
313:  const response = await fetch(`${API_BASE}${path}`, {
328:    throw new Error(data.detail || data.error || `HTTP ${response.status}`);
393:  renderCreditsPill?.();
396:function formatNumber(value) {
402:function renderCreditsPill() {
435:    <span class="credits-total">Credits ${formatNumber(totalCredits)}</span>
437:      Free ${formatNumber(freeCredits)} · Paid ${formatNumber(paidCredits)}
445:function renderAuthButtons() {
450:  renderCreditsPill();
588:                renderPage();
590:                // ignore render failures
649:renderPage();
740:function normalizedPlatformGroups(source = lastStatus) {
741:  const items = normalizedItems(source, "platform", NORMALIZED_PLATFORM_IDS);
742:  if (!items) return null; // Fallback to apiGroups() when normalized.platform is unavailable.
758:    error: 0,
774:  if (counts.error > 0) return "error";
872:      detail: "Companion chat, study grading, and context support are active.",
895:function renderCards(cards) {
908:function renderBoxes(boxes) {
921:function renderInfraCards(items) {
937:            <span>Error ${item.counts.error}</span>
954:function renderLedgerRows(items) {
970:            ${Number(item.delta || 0) >= 0 ? "+" : ""}${formatNumber(item.delta || 0)}
978:function renderReservationRows(items) {
998:                free ${formatNumber(freeAmount)} ·
999:                paid ${formatNumber(paidAmount)} ·
1005:              <b>${formatNumber(item.amount || 0)}</b>
1023:function renderApiCards(items) {
1055:  // GPU catalog is static in the mock provider, so do not refetch it on every status tick.
1077:function renderGpuOptions() {
1086:      ${safeText(item.name)} · ${formatNumber(item.vram_gb)}GB VRAM · ${moneyFromCents(item.provider_cost_cents_per_hour)}/hr provider cost
1091:function renderGpuQuoteBox() {
1105:          <p>${safeText(q.provider)} · ${formatNumber(q.duration_minutes)} minutes</p>
1116:          <strong>${formatNumber(q.credits_required)}</strong>
1140:function renderGpuReserveResult() {
1150:      Paid credits reserved: ${formatNumber(result.paid_credits_reserved || 0)}.
1198:    renderPage();
1200:    alert(err.message);
1234:    renderPage();
1236:    alert(err.message);
1279:    renderPage();
1281:    alert(err.message);
1306:function renderGpuSessionsList() {
1321:              reserved ${formatNumber(session.credits_reserved || 0)} paid credits ·
1322:              charged ${formatNumber(session.final_credits_charged || 0)} ·
1323:              billable ${formatNumber(session.billable_minutes || 0)} min
1381:    renderPage();
1383:    alert(err.message);
1419:    renderPage();
1421:    alert(err.message);
1457:    renderPage();
1459:    alert(err.message);
1466:function renderAdminCreditGrant() {
1484:          <input id="adminGrantEmail" type="email" placeholder="user@example.com" value="${safeText(user.email || "")}" />
1489:          <input id="adminGrantAmount" type="number" min="1" step="1" value="100" />
1502:          <input id="adminGrantReason" type="text" value="admin_manual_grant" />
1546:      ? `Grant ${formatNumber(amount)} PAID credits to ${email}?`
1547:      : `Grant ${formatNumber(amount)} free/local credits to ${email}?`;
1582:    alert(`Granted ${formatNumber(amount)} ${type} credits to ${email}.`);
1583:    renderPage();
1585:    alert(err.message);
1612:    supportThread = await api(`/support/tickets/${ticketId}/messages`, {
1615:    renderPage();
1617:    alert(err.message);
1621:function renderAdminUsers() {
1649:              <td>${formatNumber(u.free_credit_balance || 0)}</td>
1650:              <td>${formatNumber(u.paid_credit_balance || 0)}</td>
1660:function renderAdminSupportInbox() {
1676:              ${formatNumber(ticket.message_count || 0)} messages ·
1677:              ${safeText(ticket.last_message_at || ticket.updated_at || "")}
1691:function renderAdminSystemStatus() {
1718:        <strong>${formatNumber(nodes.length)}</strong>
1723:        <strong>${formatNumber(services.length)}</strong>
1756:function renderSupportTicketList(tickets) {
1771:              ${formatNumber(ticket.message_count || 0)} messages ·
1772:              ${safeText(ticket.last_message_at || ticket.updated_at || "")}
1786:function renderSupportThread() {
1798:      <div class="message-thread">
1799:        ${(supportThread.messages || []).map((message) => `
1800:          <div class="message-bubble ${message.sender_role === "admin" ? "admin-message" : "user-message"}">
1801:            <div class="message-meta">
1802:              ${safeText(message.sender_role)} · ${safeText(message.sender_email || "")} · ${safeText(message.created_at)}
1804:            <div>${safeText(message.body)}</div>
1810:        <textarea id="supportReplyBody" rows="4" placeholder="Write a reply..."></textarea>
1852:    renderPage();
1854:    alert(err.message);
1858:async function sendSupportReply(ticketId) {
1876:    await api(`/support/tickets/${ticketId}/messages`, {
1892:    renderPage();
1894:    alert(err.message);
1901:// Main /credits page renderer.
1906:function formatRewardCooldown(seconds) {
1934:function setGoogleRewardedMessage(message) {
1935:  googleRewardedMessage = message || "";
1936:  renderPage();
1954:      existing.addEventListener("error", () => reject(new Error("Google Publisher Tag failed to load.")), { once: true });
1960:    script.src = "https://securepubads.g.doubleclick.net/tag/js/gpt.js";
1963:    script.onerror = () => reject(new Error("Google Publisher Tag failed to load."));
2107:              setGoogleRewardedMessage(err.message || "Reward earned, but credit claim failed.");
2133:        setGoogleRewardedMessage(err.message || "Rewarded ad failed to load.");
2138:    setGoogleRewardedMessage(err.message || "Rewarded ad failed to load.");
2179:      detail: `Try again in ${formatRewardCooldown(remaining)}.`,
2180:      button: `Cooldown ${formatRewardCooldown(remaining)}`,
2211:function renderCreditsPage() {
2242:          Credits are the platform currency for companion usage, AI jobs, storage, RAG indexing,
2249:            <strong>${formatNumber(freeAvailable)}</strong>
2250:            <p>Can only be used on local platform services running on your hardware.</p>
2255:            <strong>${formatNumber(paidAvailable)}</strong>
2261:            <strong>${formatNumber(totalAvailable)}</strong>
2267:            <strong>${formatNumber(totalReserved)}</strong>
2268:            <p>Free reserved: ${formatNumber(freeReserved)} · Paid reserved: ${formatNumber(paidReserved)}</p>
2273:            <strong>${formatNumber(monthlyFree)}</strong>
2279:            <strong>${formatNumber(monthlyPaid)}</strong>
2285:            <strong>${formatNumber(storage)} MB</strong>
2315:            <strong>${formatNumber(adRewardStatus?.reward_credits || 5)} free credits</strong>
2321:            <strong>${formatNumber(adRewardStatus?.daily?.used || 0)} / ${formatNumber(adRewardStatus?.daily?.limit || 5)}</strong>
2322:            <p>Monthly: ${formatNumber(adRewardStatus?.monthly?.used || 0)} / ${formatNumber(adRewardStatus?.monthly?.limit || 100)}</p>
2389:      ${renderAdminCreditGrant()}
2396:        ${renderLedgerRows(live.ledger || [])}
2404:        ${renderReservationRows(live.reservations || [])}
2424:              ${renderGpuOptions()}
2430:            <input id="gpuQuoteDuration" type="number" min="5" max="1440" step="5" value="30" />
2438:        ${renderGpuQuoteBox()}
2439:        ${renderGpuReserveResult()}
2452:      ${loggedIn ? renderGpuSessionsList() : `<div class="notice">Log in to view GPU sessions.</div>`}
2458:        Draft tiers for testing the business model. Checkout will be connected later.
2465:          <p>Good for trying the platform.</p>
2482:            <li>More companion messages</li>
2536:function renderSystemPage() {
2538:  const platformGroups = normalizedPlatformGroups() || apiGroups();
2545:        Public users can see whether platform API areas are online, degraded, offline, or planned.
2547:      ${renderApiCards(platformGroups)}
2556:        ${renderInfraCards(infraGroups)}
2569:function renderPage() {
2573:  document.title = `${page.title} | AlexHartel AI Platform`;
2576:  renderAuthButtons();
2592:      ${page.cards ? renderCards(page.cards) : ""}
2593:      ${page.boxes?.length ? renderBoxes(page.boxes) : ""}
2594:      ${isCredits ? renderCreditsPage() : ""}
2596:      ${isSystem ? renderSystemPage() : ""}
2601:    btn.addEventListener("click", () => navigate(btn.getAttribute("data-go")));
2604:  $("openSystemBtn")?.addEventListener("click", openSystemDrawer);
2605:  $("wakeLoginBtn")?.addEventListener("click", () => {
2610:  $("claimAdRewardBtn")?.addEventListener("click", claimMockAdReward);
2611:  $("showGoogleRewardedAdBtn")?.addEventListener("click", loadGoogleRewardedAd);
2612:  $("gpuQuoteBtn")?.addEventListener("click", quoteMockGpuSession);
2613:  $("gpuReserveQuoteBtn")?.addEventListener("click", reserveMockGpuQuote);
2614:  $("adminGrantCreditsBtn")?.addEventListener("click", adminGrantCredits);
2616:  $("supportCreateTicketBtn")?.addEventListener("click", createSupportTicket);
2617:  $("supportReplyBtn")?.addEventListener("click", (event) => {
2618:    sendSupportReply(event.currentTarget.dataset.ticketId);
2622:    button.addEventListener("click", () => loadSupportThread(button.dataset.openTicket));
2625:  $("gpuStartSessionBtn")?.addEventListener("click", (buttonEvent) => {
2631:    button.addEventListener("click", () => stopMockGpuSession(button.dataset.stopGpuSession));
2634:    button.addEventListener("click", () => cleanupMockGpuSession(button.dataset.cleanupGpuSession));
2637:    button.addEventListener("click", () => refundReservationToken(button.dataset.refundToken));
2641:function renderDrawerItems(targetId, items, type) {
2665:        `Total ${item.counts.total}, Online ${item.counts.online}, Offline ${item.counts.offline}, Booting ${item.counts.booting}, Error ${item.counts.error}. ${item.detail}`;
2684:function renderSystemDrawer() {
2686:  const platformGroups = normalizedPlatformGroups() || apiGroups();
2693:    renderDrawerItems("drawerNodes", infraGroups, "infra");
2695:    renderDrawerItems("drawerNodes", [], "api");
2698:  renderDrawerItems("drawerServices", platformGroups, "api");
2703:  renderSystemDrawer();
2758:    renderPage();
2760:    alert(err.message);
2823:      error: err.message,
2832:  renderPage();
2833:  renderSystemDrawer();
2845:function ensureResendVerificationButton() {
2849:  let btn = $("resendVerificationBtn");
2852:    btn.id = "resendVerificationBtn";
2856:    btn.textContent = "Resend verification email";
2857:    btn.addEventListener("click", resendVerificationEmail);
2868:  ensureResendVerificationButton();
2878:    const submitBtn = $("authSubmitBtn");
2879:    if (submitBtn && submitBtn.parentNode) {
2880:      submitBtn.parentNode.insertBefore(el, submitBtn.nextSibling);
2889:function setEmailVerificationMessage(message, isError = false) {
2893:  el.textContent = message || "";
2894:  el.classList.toggle("error", Boolean(isError));
2895:  el.hidden = !message;
2949:    data?.message || "Check your email to finish creating your account."
2952:  const resendBtn = $("resendVerificationBtn");
2953:  if (resendBtn) {
2954:    resendBtn.hidden = false;
2958:async function resendVerificationEmail() {
2966:  const btn = $("resendVerificationBtn");
2973:    const data = await api("/auth/resend-verification", {
2980:      data?.message || "Verification email sent. Check your inbox."
2983:    setEmailVerificationMessage(err.message || "Could not resend verification email.", true);
2987:      btn.textContent = "Resend verification email";
2992:function showPageNotice(message, isError = false) {
3006:  el.textContent = message || "";
3007:  el.classList.toggle("error", Boolean(isError));
3008:  el.hidden = !message;
3066:    const message = err.message || "Email verification failed.";
3068:    showPageNotice(message, true);
3071:    setEmailVerificationMessage(message, true);
3088:      ? "Create an account to use platform services."
3100:  ensureResendVerificationButton();
3164:    renderPage();
3166:    $("authMessage").textContent = err.message;
3191:  renderPage();
3196:    renderAuthButtons();
3212:  renderAuthButtons();
3214:  renderPage();
3217:document.addEventListener("click", (event) => {
3228:window.addEventListener("popstate", renderPage);
3230:$("drawerCloseBtn").addEventListener("click", closeSystemDrawer);
3231:$("authOpenBtn").addEventListener("click", () => openAuthModal("login"));
3232:$("logoutBtn").addEventListener("click", logout);
3233:$("authCloseBtn").addEventListener("click", closeAuthModal);
3234:$("loginTabBtn").addEventListener("click", () => setAuthMode("login"));
3235:$("registerTabBtn").addEventListener("click", () => setAuthMode("register"));
3236:$("resendVerificationBtn")?.addEventListener("click", resendVerificationEmail);
3237:$("authForm").addEventListener("submit", handleAuthSubmit);
3239:renderPage();
3255:// renderSupportPage()/renderAdminPage() functions.
3314:    await sendWebPresence({ force: true });
3316:    // ignore presence errors
3329:    cleanSupportTickets = { ok: false, tickets: [], detail: err.message };
3355:      error: err.message,
3378:    : { ok: false, users: [], detail: users.reason?.message || "Failed to load users." };
3382:    : { ok: false, tickets: [], detail: tickets.reason?.message || "Failed to load tickets." };
3386:    : { ok: false, nodes: [], services: [], detail: system.reason?.message || "Failed to load system status." };
3415:              ${cleanNum(ticket.message_count || 0)} messages ·
3416:              ${cleanEsc(ticket.last_message_at || ticket.updated_at || "")}
3434:    cleanOpenThread = await api(`/support/tickets/${ticketId}/messages`, { method: "GET" });
3437:    alert(err.message);
3445:  const messages = cleanOpenThread.messages || [];
3454:      <div class="message-thread">
3455:        ${messages.map((m) => `
3456:          <div class="message-bubble ${m.sender_role === "admin" ? "admin-message" : "user-message"}">
3457:            <div class="message-meta">
3458:              ${cleanEsc(m.sender_role)} · ${cleanEsc(m.sender_email || "")} · ${cleanEsc(m.created_at)}
3466:        <textarea id="cleanSupportReplyBody" rows="4" placeholder="Write a reply..."></textarea>
3531:    alert(err.message);
3576:    alert(err.message);
3589:    await api(`/support/tickets/${ticketId}/messages`, {
3603:    alert(err.message);
3620:        <h3>Send a message</h3>
3621:        <p>Logged-in users can create a support ticket with a subject and message.</p>
3631:        <p>Each ticket keeps the full conversation in one place so users and admins have context.</p>
3662:          <span>Platform issues</span>
3664:          <p>Report slow pages, broken buttons, offline services, or unexpected errors.</p>
3679:          Log in to send a message to customer support and view your ticket history.
3692:      <p>Send a message if you need account, credit, billing, study, companion, or platform help.</p>
3697:      <div class="support-form">
3700:          <input id="cleanSupportSubject" type="text" placeholder="Short description of the issue" />
3705:          <textarea id="cleanSupportBody" rows="5" placeholder="Describe what happened and what you expected."></textarea>
3709:          Send message
3728:    error: 0,
3754:        Error ${cleanNum(counts.error)}
3763:  if (nodes.some((n) => n.state === "error")) return "error";
3895:          <input id="cleanAdminGrantEmail" type="email" placeholder="user@example.com" value="${cleanEsc(email)}" />
3900:          <input id="cleanAdminGrantAmount" type="number" min="1" step="1" value="100" />
3913:          <input id="cleanAdminGrantReason" type="text" value="admin_manual_grant" />
3968:    alert(err.message);
4043:        Resolved support conversations are kept here for history.
4066:        <div class="notice error">Could not load power policy: ${cleanEsc(policy.error || "Unknown error")}</div>
4236:  document.getElementById("cleanCreateSupportTicketBtn")?.addEventListener("click", cleanCreateSupportTicket);
4237:  document.getElementById("cleanAdminGrantCreditsBtn")?.addEventListener("click", cleanAdminGrantCredits);
4242:    btn.addEventListener("click", () => cleanOpenTicket(btn.dataset.cleanOpenTicket));
4248:    btn.addEventListener("click", () => cleanSendReply(btn.dataset.cleanReplyTicket));
4254:    btn.addEventListener("click", () => cleanSetTicketStatus(btn.dataset.cleanSolveTicket, "solved"));
4260:    btn.addEventListener("click", () => cleanSetTicketStatus(btn.dataset.cleanReopenTicket, "waiting_admin"));
4266:    btn.addEventListener("click", () => openAuthModal("login"));
4269:  document.getElementById("cleanToggleAdminSystemDetailsBtn")?.addEventListener("click", () => {
4278:  cleanOriginalRenderPage = renderPage;
4280:  renderPage = function(...args) {
4304:  console.warn("Clean admin/support render wrapper failed:", err);
4309:  renderPage();
4312:document.addEventListener("click", async (event) => {
4324:  renderPage();
4335:  renderPage();
4369:      renderPage();
4420:// Login renders immediately after auth + /me.
4427:// Login: auth -> /me -> render now -> refresh page data in background.
4433:function fastSetAuthMessage(message, isError = false) {
4436:    if (message && isError) alert(message);
4440:  box.textContent = message || "";
4441:  box.classList.toggle("hidden", !message);
4442:  box.classList.toggle("error", Boolean(isError));
4458:  if (typeof sendWebPresence === "function" && authState?.token) {
4459:    jobs.push(sendWebPresence("force"));
4481:    renderPage();
4483:    // ignore render errors
4488:  const form = event.target;
4490:  if (!form || form.id !== "authForm") {
4501:  const submitBtn = document.getElementById("authSubmitBtn");
4506:  if (submitBtn) {
4507:    submitBtn.disabled = true;
4508:    submitBtn.textContent = mode === "register" ? "Sending..." : "Logging in...";
4546:    renderPage();
4551:    fastSetAuthMessage(err.message || "Login failed.", true);
4555:    if (submitBtn) {
4556:      submitBtn.disabled = false;
4557:      submitBtn.textContent = mode === "register" ? "Send verification email" : "Login";
4586:    renderPage();
4588:    // ignore render errors
4593:    fetch(`${API_BASE}/auth/logout`, {
4605:document.addEventListener("submit", fastHandleAuthSubmit, true);
4606:document.addEventListener("click", fastHandleLogoutClick, true);
4640:async function sendWebPresence(reason = "") {
4657:  // Debounce normal presence sends.
4689:  sendWebPresence(authState?.token ? "startup-logged-in" : "startup-anonymous-check");
4693:  sendWebPresence("15-second-intent");
4697:  sendWebPresence("interval");
4702:    sendWebPresence("force");
4706:// Strong signal when user opens auth modal or clicks login/register.
4707:document.addEventListener("click", (event) => {
4712:    sendWebPresence("force");
4771:  if (typeof sendWebPresence === "function" && !window.__ahWebPresenceApplyWrapped) {
4773:    window.__ahOriginalSendWebPresence = sendWebPresence;
4775:    sendWebPresence = async function(reason = "") {
4794:    renderCreditsPill();
4813:    renderCreditsPill();
4816:    renderPage();
4817:    sendWebPresence?.("force");
4824:    renderCreditsPill();
4827:    renderPage();
4837:if (document.readyState === "loading") {
4845:document.addEventListener("click", (event) => {
4894:    btn.addEventListener("click", requestPasswordResetFromLogin);
4896:    const resendBtn = $("resendVerificationBtn");
4897:    const submitBtn = $("authSubmitBtn");
4899:    if (resendBtn && resendBtn.parentNode) {
4900:      resendBtn.parentNode.insertBefore(btn, resendBtn.nextSibling);
4901:    } else if (submitBtn && submitBtn.parentNode) {
4902:      submitBtn.parentNode.insertBefore(btn, submitBtn.nextSibling);
4923:    setEmailVerificationMessage("Enter your email address first, then click Forgot password.", true);
4940:      data?.message || "If that email exists, a password reset link has been sent."
4943:    setEmailVerificationMessage(err.message || "Could not request password reset.", true);
4997:    showPageNotice(data?.message || "Password reset. You can now log in.");
5006:    const message = err.message || "Password reset failed.";
5007:    showPageNotice(message, true);
5010:    setEmailVerificationMessage(message, true);
5041: * Stage 5F-31: queued-chat frontend flag detection.
5043: * This block intentionally does not wire queued chat send behavior.
5044: * It only records whether the disabled-by-default queued-chat frontend flag is enabled.
5047: * - queued chat remains disabled by default
5048: * - legacy/current chat path remains active while flag is false
5049: * - does not call the queued chat API route
5050: * - does not use the queued chat status helper yet
5051: * - does not send client-provided user identity fields
5052: * - does not send synthetic-user headers
5061:    source: "app_js_queued_chat_flag_detection",
5069: * Stage 5F-32: disabled queued-chat send branch.
5071: * This block defines a future queued-chat send helper, but intentionally does not
5072: * wire it into the current chat submit flow.
5075: * - branch is gated by the disabled-by-default queued-chat frontend flag
5076: * - branch is not wired to submit
5077: * - legacy/current chat path remains active while flag is false
5078: * - does not send client-provided user identity fields
5079: * - does not send synthetic-user headers
5091:        reason: "queued_chat_disabled_stage_5f32",
5097:      message: String((payload && payload.message) || ""),
5100:    if (payload && payload.chat_id) {
5101:      cleanPayload.chat_id = String(payload.chat_id);
5104:    if (payload && payload.requested_model) {
5105:      cleanPayload.requested_model = String(payload.requested_model);
5108:    const response = await fetch("/api/chat/queued", {
5123:        error: data,
5132:    source: "app_js_disabled_queued_send_branch",
5134:    sendQueuedChat: stage5f32SendQueuedChat,
5139: * Stage 5F-35: disabled queued-chat status polling branch.
5141: * This block defines a future queued-chat status polling helper, but intentionally
5142: * does not wire it into the current chat submit flow or any runtime polling loop.
5145: * - branch is gated by the disabled-by-default queued-chat frontend flag
5146: * - branch is not wired to submit
5148: * - legacy/current chat path remains active while flag is false
5149: * - does not send client-provided identity fields
5150: * - does not send synthetic-user headers
5172:        error: "queued_status_helper_missing_stage_5f35",
5181:        error: "missing_job_id_stage_5f35",
5185:    const response = await fetch(`/api/chat/queued/${encodeURIComponent(cleanJobId)}`, {
5199:        error: data,
5224: * Stage 5F-37: disabled queued-chat assistant placeholder branch.
5226: * This block defines a future queued-chat assistant placeholder helper, but
5227: * intentionally does not wire it into the current chat rendering flow.
5230: * - branch is gated by the disabled-by-default queued-chat frontend flag
5231: * - branch is not wired to submit
5232: * - branch is not wired to message rendering
5233: * - legacy/current chat path remains active while flag is false
5234: * - does not send client-provided identity fields
5235: * - does not send synthetic-user headers
5257:        error: "queued_placeholder_helper_missing_stage_5f37",
5270:      assistantReply: view.assistantReply || "",
5276:    source: "app_js_disabled_queued_assistant_placeholder_branch",
5283: * Stage 5F-40: disabled queued-chat submit decision branch.
5285: * This block defines a future decision helper for selecting the queued-chat
5286: * submit path, but intentionally does not wire it into the current submit flow.
5289: * - decision is gated by the disabled-by-default queued-chat frontend flag
5291: * - legacy/current chat path remains active
5292: * - does not submit jobs
5294: * - does not send client-provided identity fields
5295: * - does not send synthetic-user headers
5302:    const sendBranch = root.AI_PLATFORM_QUEUED_CHAT_SEND_BRANCH;
5310:        reason: "queued_chat_flag_disabled_stage_5f40",
5318:     * Even if a mocked send branch reports wiredToSubmit=true, queued submit
5326:        reason: "queued_chat_decision_not_wired_stage_5f41",
5332:    if (!sendBranch || sendBranch.wiredToSubmit !== true) {
5337:        reason: "queued_chat_submit_not_wired_stage_5f40",
5347:      reason: "queued_chat_submit_selected_stage_5f40",
5351:        hasMessage: Boolean(context && String(context.message || "").trim()),
5352:        hasChat: Boolean(context && context.chat_id),
5353:        hasModel: Boolean(context && context.requested_model),
5360:    source: "app_js_disabled_queued_submit_decision_branch",

## app.js queued helper branch markers
5041: * Stage 5F-31: queued-chat frontend flag detection.
5054:(function stage5f31QueuedChatFlagDetection(root) {
5057:  const enabled = root.AI_PLATFORM_QUEUED_CHAT_ENABLED === true;
5059:  root.AI_PLATFORM_QUEUED_CHAT_UI_STATE = Object.freeze({
5061:    source: "app_js_queued_chat_flag_detection",
5069: * Stage 5F-32: disabled queued-chat send branch.
5081:(function stage5f32QueuedChatSendBranch(root) {
5084:  async function stage5f32SendQueuedChat(payload) {
5085:    const enabled = root.AI_PLATFORM_QUEUED_CHAT_ENABLED === true;
5091:        reason: "queued_chat_disabled_stage_5f32",
5130:  root.AI_PLATFORM_QUEUED_CHAT_SEND_BRANCH = Object.freeze({
5134:    sendQueuedChat: stage5f32SendQueuedChat,
5139: * Stage 5F-35: disabled queued-chat status polling branch.
5152:(function stage5f35QueuedChatStatusPollBranch(root) {
5155:  async function stage5f35PollQueuedChatStatus(jobId, options) {
5156:    const enabled = root.AI_PLATFORM_QUEUED_CHAT_ENABLED === true;
5215:  root.AI_PLATFORM_QUEUED_CHAT_STATUS_POLL_BRANCH = Object.freeze({
5219:    pollQueuedChatStatus: stage5f35PollQueuedChatStatus,
5224: * Stage 5F-37: disabled queued-chat assistant placeholder branch.
5237:(function stage5f37QueuedChatAssistantPlaceholderBranch(root) {
5240:  function stage5f37BuildQueuedAssistantPlaceholder(job, options) {
5241:    const enabled = root.AI_PLATFORM_QUEUED_CHAT_ENABLED === true;
5274:  root.AI_PLATFORM_QUEUED_CHAT_ASSISTANT_PLACEHOLDER_BRANCH = Object.freeze({
5278:    buildQueuedAssistantPlaceholder: stage5f37BuildQueuedAssistantPlaceholder,
5283: * Stage 5F-40: disabled queued-chat submit decision branch.
5297:(function stage5f40QueuedChatSubmitDecisionBranch(root) {
5300:  function stage5f40ShouldUseQueuedChatForSubmit(context) {
5301:    const enabled = root.AI_PLATFORM_QUEUED_CHAT_ENABLED === true;
5302:    const sendBranch = root.AI_PLATFORM_QUEUED_CHAT_SEND_BRANCH;
5310:        reason: "queued_chat_flag_disabled_stage_5f40",
5317:     * Stage 5F-41: decisionWired remains the authoritative guard.
5326:        reason: "queued_chat_decision_not_wired_stage_5f41",
5337:        reason: "queued_chat_submit_not_wired_stage_5f40",
5347:      reason: "queued_chat_submit_selected_stage_5f40",
5358:  root.AI_PLATFORM_QUEUED_CHAT_SUBMIT_DECISION_BRANCH = Object.freeze({
5362:    shouldUseQueuedChatForSubmit: stage5f40ShouldUseQueuedChatForSubmit,

## index.html chat/control/script markers
6:  <title>AlexHartel AI Platform</title>
11:    <a class="brand logo-only" href="/" data-route="/" aria-label="AlexHartel AI Platform home">
24:      <a href="/chat" data-route="/chat">Chat</a>
51:  <aside id="systemDrawer" class="system-drawer hidden" aria-label="System status drawer">
63:    <div class="status-grid">
66:        <div id="drawerNodes" class="status-list"></div>
71:        <div id="drawerServices" class="status-list"></div>
94:      <form id="authForm" class="auth-form">
97:          <input id="authEmail" type="email" autocomplete="email" required />
102:          <input id="authPassword" type="password" autocomplete="current-password" required />
105:        <button id="authSubmitBtn" class="primary-btn" type="submit">
108:      </form>
114:  <script src="/app.js?v=20260605145139"></script>
115:  <script src="/queued_chat_config.js"></script>
116:<script src="/queued_chat_status.js"></script>

## dev_server.py chat/proxy markers
18:FULL_APP_ROUTES = {"/study", "/chat", "/companion", "/calendar", "/profile"}
19:WRAPPER_ROUTES = {"/", "/study", "/chat", "/companion", "/calendar", "/profile", "/system"}
26:    controller_auth_exact = {
27:        "/api/me": "/system/session/me",
28:        "/api/auth/login": "/system/session/login",
29:        "/api/auth/register": "/system/session/register",
30:        "/api/auth/logout": "/system/session/logout-safe",
32:        # Keep browser auth flows on the controller instead of falling through
34:        "/api/auth/forgot-password": "/system/session/forgot-password",
35:        "/api/auth/reset-password": "/system/session/reset-password",
36:        "/api/auth/change-password": "/system/session/change-password",
37:        "/api/auth/verify-email": "/api/auth/verify-email",
38:        "/api/auth/resend-verification": "/api/auth/resend-verification",
43:    if path in controller_auth_exact:
44:        return CONTROLLER, controller_auth_exact[path]
51:        "/api/me": "/system/session/me",
52:        "/api/session/presence": "/system/session/presence",
56:        "/api/auth/login": "/system/session/login",
57:        "/api/auth/register": "/system/session/register",
58:        "/api/auth/logout": "/system/session/logout-safe",
66:        "/api/gpu/sessions": "/system/gpu/sessions",
68:        "/api/gpu/stop-session": "/system/gpu/stop-session",
69:        "/api/gpu/cleanup-mock-session": "/system/gpu/cleanup-mock-session",
122:    def _auth_route_token(self):
135:    def _has_auth_route_cookie(self):
136:        return bool(self._auth_route_token())
138:    def _should_proxy_full_app(self, path):
139:        if not self._has_auth_route_cookie():
151:        token = self._auth_route_token()
153:            auth = self.headers.get("Authorization", "")
154:            if auth.lower().startswith("bearer "):
155:                token = auth.split(" ", 1)[1].strip()
161:            CONTROLLER + "/system/session/me",
174:        auth_bridge_path = original_path or upstream_path
175:        if not auth_bridge_path.startswith("/api/backend/"):
191:    def _proxy_to_backend(self, backend, upstream_path, query="", original_path=None):
201:        headers = {"User-Agent": "wrapper-proxy/3.0"}
221:        # browser path so cookie-to-bearer auth still happens after mapping.
222:        auth_source_path = original_path or upstream_path
223:        if auth_source_path.startswith("/api/backend/") and not headers.get("Authorization"):
224:            token = self._auth_route_token()
286:    def _proxy_api(self):
289:        return self._proxy_to_backend(backend, upstream_path, parsed.query, original_path=parsed.path)
311:            return self._proxy_api()
313:        if self._should_proxy_full_app(path):
314:            return self._proxy_to_backend(CT101_FRONTEND, path, parsed.query)
328:            return self._proxy_api()
330:        if self._should_proxy_full_app(path):
331:            return self._proxy_to_backend(CT101_FRONTEND, path, parsed.query)
340:            return self._proxy_api()
355:            return self._proxy_api()
